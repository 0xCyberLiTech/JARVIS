# requirements.txt — JARVIS
# Généré 2026-05-12 depuis pip freeze + audit imports Python (jarvis.py + soc.py + jarvis_mcp_server.py)
# Hardware cible : Windows 11 + Python 3.11 + RTX 5080 Blackwell (CUDA 12)
# Pour réinstaller : pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────
Flask==3.1.3
Flask-Limiter==4.1.1
flask-sock==0.7.0
Werkzeug==3.1.6

# ── HTTP / réseau ─────────────────────────────────────────────────────────
requests==2.32.5
httpx==0.28.1
urllib3==2.6.3
paramiko==4.0.0

# ── ML core (PyTorch CUDA) ────────────────────────────────────────────────
# IMPORTANT : torch CUDA 12.8 spécifique RTX 5080 sm_120 — ne pas downgrader
torch==2.7.1+cu128
numpy==1.26.4
scipy==1.17.1

# ── STT (Speech-to-Text) ──────────────────────────────────────────────────
faster-whisper==1.2.1
ctranslate2==4.7.1

# ── TTS (Text-to-Speech) — edge-tts (cloud) + Kokoro (repli local CUDA) ───
# Piper + SAPI5 (pyttsx3) retirés 2026-06-17 (code mort legacy)
edge-tts==7.2.7
kokoro==0.9.4

# ── Audio ─────────────────────────────────────────────────────────────────
DeepFilterNet==0.5.6
librosa==0.11.0
soundfile==0.13.1
miniaudio==1.61

# ── RAG ───────────────────────────────────────────────────────────────────
rank-bm25==0.2.2
huggingface_hub==1.7.1

# ── MCP server ────────────────────────────────────────────────────────────
mcp==1.27.0
starlette==0.52.1
uvicorn==0.46.0

# ── System / monitoring ───────────────────────────────────────────────────
psutil==7.2.2
nvidia-ml-py==13.590.48     # fournit le module `pynvml` (GPU monitoring)
loguru==0.7.3
